#include <stdio.h>
int main()
{
   int A[100]={0},i,j,n;
   scanf("%d",&n);
   for (i=2;i<n;i++)
   {
   	  for (j=2;i*j<n;j++)
   	  {
   		A[i*j]=1;
	    }
	    if (A[i]==1) continue;
	    else printf("%d ",i);
   }
   return 0;
}
